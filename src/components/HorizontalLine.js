import React from 'react';
import { View } from 'react-native';

const HorizontalLine = props => (
  <View style={{ borderBottomColor: 'green', borderBottomWidth: 1 }} />
);

export default HorizontalLine;
